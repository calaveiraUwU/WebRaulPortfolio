---
import ProjectGalleryClient from './ProjectGalleryClient.jsx';

const { title, type, desc, tech, media = [], highlights = [], links = [] } = Astro.props;
---
<div class="single-item">
    {media.length > 0 && <ProjectGalleryClient client:load media={media} />}

  <h3>{title}</h3>
  {type && <p class="project-type">{type}</p>}
  <!-- <div class="project-desc project-desc-list w-richtext">
    <ul role="list">
      {desc.map(item => <li>{item}</li>)}
    </ul>
  </div>

  <div class="mini-label-white">Technologies & Tools</div>
  <div class="chips-container">
    {tech.map(techItem => <div class="single-chip">{techItem}</div>)}
  </div> -->
</div>
